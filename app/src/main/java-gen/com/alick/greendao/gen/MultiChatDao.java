package com.alick.greendao.gen;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import demo.greendao.acewill.com.greendao3_demo.bean.Linkman_MultiChat;

import demo.greendao.acewill.com.greendao3_demo.bean.MultiChat;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MULTI_CHAT".
*/
public class MultiChatDao extends AbstractDao<MultiChat, String> {

    public static final String TABLENAME = "MULTI_CHAT";

    /**
     * Properties of entity MultiChat.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property MultiChatId = new Property(0, String.class, "multiChatId", true, "MULTI_CHAT_ID");
        public final static Property MultiChatName = new Property(1, String.class, "multiChatName", false, "MULTI_CHAT_NAME");
    }

    private DaoSession daoSession;

    private Query<MultiChat> linkman_MultiChatsQuery;

    public MultiChatDao(DaoConfig config) {
        super(config);
    }
    
    public MultiChatDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MULTI_CHAT\" (" + //
                "\"MULTI_CHAT_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: multiChatId
                "\"MULTI_CHAT_NAME\" TEXT);"); // 1: multiChatName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MULTI_CHAT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MultiChat entity) {
        stmt.clearBindings();
 
        String multiChatId = entity.getMultiChatId();
        if (multiChatId != null) {
            stmt.bindString(1, multiChatId);
        }
 
        String multiChatName = entity.getMultiChatName();
        if (multiChatName != null) {
            stmt.bindString(2, multiChatName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MultiChat entity) {
        stmt.clearBindings();
 
        String multiChatId = entity.getMultiChatId();
        if (multiChatId != null) {
            stmt.bindString(1, multiChatId);
        }
 
        String multiChatName = entity.getMultiChatName();
        if (multiChatName != null) {
            stmt.bindString(2, multiChatName);
        }
    }

    @Override
    protected final void attachEntity(MultiChat entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public MultiChat readEntity(Cursor cursor, int offset) {
        MultiChat entity = new MultiChat( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // multiChatId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // multiChatName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MultiChat entity, int offset) {
        entity.setMultiChatId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setMultiChatName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final String updateKeyAfterInsert(MultiChat entity, long rowId) {
        return entity.getMultiChatId();
    }
    
    @Override
    public String getKey(MultiChat entity) {
        if(entity != null) {
            return entity.getMultiChatId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MultiChat entity) {
        return entity.getMultiChatId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "multiChats" to-many relationship of Linkman. */
    public List<MultiChat> _queryLinkman_MultiChats(String linkmanId) {
        synchronized (this) {
            if (linkman_MultiChatsQuery == null) {
                QueryBuilder<MultiChat> queryBuilder = queryBuilder();
                queryBuilder.join(Linkman_MultiChat.class, Linkman_MultiChatDao.Properties.MultiChatId)
                    .where(Linkman_MultiChatDao.Properties.LinkmanId.eq(linkmanId));
                linkman_MultiChatsQuery = queryBuilder.build();
            }
        }
        Query<MultiChat> query = linkman_MultiChatsQuery.forCurrentThread();
        query.setParameter(0, linkmanId);
        return query.list();
    }

}
